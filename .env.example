APP_URL="http://localhost:3000"
EXCEL_PATH=""

# GitHub shared deposit sync (admin upload writes gh-pages/data/latest.json)
NEXT_PUBLIC_GITHUB_REPO="liuchiwai0101/FFin"
NEXT_PUBLIC_GITHUB_BRANCH="gh-pages"
NEXT_PUBLIC_GITHUB_DATA_PATH="data/latest.json"

# PAT is pasted in the Sync page UI (stored in browser localStorage), not here.
